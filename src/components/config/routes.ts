import { RouteLocation } from './types'

export const paths = {
  home: '/',
  details: '/details',
  about: '/about',
  help: '/help',
  table: '/table',
  explore: '/Explore',
} as {
  [key: string]: RouteLocation
}
