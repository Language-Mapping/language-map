import { RouteLocation } from './types'

export const paths = {
  home: '/',
  details: '/details',
  about: '/about',
  help: '/help',
  table: '/table',
  grid: '/Categories',
} as {
  [key: string]: RouteLocation
}
