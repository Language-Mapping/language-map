import { RouteLocation } from './types'

export const paths = {
  home: '/',
  details: '/details',
  about: '/about',
  help: '/help',
  table: '/table',
  grid: '/grid',
} as {
  [key: string]: RouteLocation
}
