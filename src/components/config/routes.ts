import { RouteLocation } from './types'

export const paths = {
  home: '/',
  details: '/details',
  about: '/about',
  help: '/help',
  table: '/table',
  grid: '/Explore',
} as {
  [key: string]: RouteLocation
}
