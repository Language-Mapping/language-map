import { RouteLocation } from './types'

export const paths = {
  home: '/',
  details: '/details',
  table: '/table',
  about: '/about',
  glossary: '/glossary',
} as {
  [key: string]: RouteLocation
}
